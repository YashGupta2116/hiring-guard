"""Offline calibration: labelled fixtures in, `weights.json` out.

Nothing here is on the runtime path. `tests/test_runtime_deps.py` excludes
this package by name, which is what lets `fit.py` import scikit-learn.
"""
